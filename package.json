{
  "name": "nodejs-restapi-poc",
  "version": "1.0.0",
  "description": "A NodeJS REST API poc",
  "main": "app.js",
  "scripts": {
    "test": "jest",
    "start": "nodemon app.js",  // for development (with automatic restarts)
    "start-dev": "nodemon app.js",  // same as 'start', can be used interchangeably
    "build": "echo 'No build step defined. Only install dependencies.'",  // Placeholder if no build step is required
    "install-dependencies": "npm install"  // Dedicated install dependencies script
  },
  "author": "Jonathan Spruance",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "winston": "^3.2.1",
    "yup": "^0.26.10"
  },
  "devDependencies": {
    "jest": "^24.7.1",
    "moxios": "^0.4.0",
    "nodemon": "^1.18.11",
    "supertest": "^4.0.0"
  }
}
